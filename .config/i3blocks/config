align=center
separator_block_width=15
markup=none
command=~/.config/i3blocks/$BLOCK_NAME

[progress]
command=tail -n 1 /tmp/bar$(date +%F)
interval=once
signal=11

[notes]
command=head -n 1 /tmp/todo$(date +%F)
interval=once
signal=10

[keyboard]
interval=persist

[host]
interval=once
