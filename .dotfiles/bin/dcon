#!/bin/bash
print_usage () {
echo "
Usage: $0 [OPTIONS] dcon
Wrapper for connecting into a running docker container
$ dcon <container_name>
"
}
if [ $# -eq 0 ]
then
echo $*
print_usage
exit
fi
echo $3
cmd="docker exec -ti \"$1\" /bin/bash"
echo "Connecting to \"$1\"..."
eval $cmd
