separator_block_width=15

[keyboard]
command=xkb-switch -p ; xkb-switch -W
interval=persist

[time]
command=date +"%A, %d %B %Y, %k:%M"
interval=1

[host]
command=echo $(id -un)@$(hostname)
interval=once
